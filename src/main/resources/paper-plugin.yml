load: STARTUP
main: com.DiscordLogger.Main
api-version: 1.21
prefix: "[DiscordLogger]"
version: 1.1-ALPHA
name: DiscordLogger
description: "A plugin that logs specific server events to a Discord webhook."
commands:
  discordlogger:
    description: Reload the plugin configuration
    aliases: [dlreload]